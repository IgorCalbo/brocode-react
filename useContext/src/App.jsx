// useContext() = Um react hook que permite o compartilhamento de valores entre multíplos níveis de componentes sem utilizar props

import ComponentA from "./ComponentA";

function App() {

  return(<ComponentA/>);
}

export default App
